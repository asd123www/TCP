#include <stdio.h>
#include "checkpoints/device.h"


char name[10];


int main(int argc, char *argv[]) {
    test();

    /*
    printf("argc: %d\n", argc);
    for(int i=0; i<argc; ++i) {
        printf("argument[%d]: %s\n", i, argv[i]);
    }
    */
}